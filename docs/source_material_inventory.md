# Source Material Inventory

Reviewed source root:

- `MASTER/SEGUNDO_CUATRI/MINERIA DE DATOS`
- `MASTER/SEGUNDO_CUATRI/MINERIA DE DATOS/TRABAJO`

Public-safe material staged here:

- the main coursework R Markdown file;
- the companion `_main.Rmd`;
- a reference classification script;
- a compact regression workflow script;
- selected safe PNG figures.

Excluded:

- `.xlsx`, `.csv`, `.rds`, `.RData`;
- presentation decks and coursework PDFs;
- raw and imputed data tables;
- serialized trained-model objects.

Reasoning:

The source folder contains strong modelling work, but much of it depends on private or non-distributable course data. The public repo therefore keeps the modelling logic and the report structure while excluding raw datasets and saved model objects.
